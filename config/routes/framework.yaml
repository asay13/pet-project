controllers:
    resource:
        path: ../../src/Controller
        namespace: App\Controller
    type: attribute
    prefix: /v1

when@dev:
    _errors:
        resource: '@FrameworkBundle/Resources/config/routing/errors.xml'
        prefix: /_error
